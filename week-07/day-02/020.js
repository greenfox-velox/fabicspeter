'use strict';

// create a function called apply that takes a function
// and calls it with one argument
// that is the string 'apple'

var printer = function (a) {
  return a;
};

console.log(printer('apple')); // 3
